# Example functions to be called from main.c
# To export as an object file 
# ./tial src.tial
def sum(a, b, c)
	let l = a + b + c ; #local sum 
	return l;

def foo(a)
	let sum = sum(a,a,a)-3;
	return sum+3-2;
