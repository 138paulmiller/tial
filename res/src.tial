# Example functions to be called from main.c
# To export as an object file 
# ./tial src.tial
def sum(a, b, c)
	let l = a + b + c ; #local sum 
	return l;

def foo(a)
	let sum = sum(a*2,a*2,a*2)-sum(a, a, a);
	return 3*sum-sum*2-sum*2;
